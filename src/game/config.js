pandaConfig = {
  sourceFolder: 'src',
  mediaFolder: 'media',
  outputFile: 'game.min.js',
  system: {
    orientation: 1,
    width: $(window).width(),
    height: $(window).height(),
    canvasId: 'game-canvas',
    bgColor: 'f3ede4',
  }
};
